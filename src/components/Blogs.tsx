// ? Local Files

// ? packages

export default function Blogs() {
  return (
    <section
      id="blogs"
      className="w-full h-full overflow-hidden grid place-items-center"
    >
      Blogs
    </section>
  );
}
